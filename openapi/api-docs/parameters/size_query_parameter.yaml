name: size
in: query
schema:
  type: integer
  minimum: 1
  multipleOf: 1
required: false
description: |-
  The number of records retrieved per page. Defaults to 20 and goes up to 1000, even if
  it's set to a number greater than 1000.
